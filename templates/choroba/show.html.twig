{% extends 'base.html.twig' %}

{% block title %}Choroba{% endblock %}

{% block body %}
    <h1>Choroba</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choroba.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ choroba.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('choroba_index') }}">back to list</a>

    <a href="{{ path('choroba_edit', {'id': choroba.id}) }}">edit</a>

    {{ include('choroba/_delete_form.html.twig') }}
{% endblock %}
