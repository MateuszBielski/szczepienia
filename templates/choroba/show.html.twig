{% extends 'base.html.twig' %}

{% block title %}Choroba{% endblock %}

{% block body %}
    <h3>Choroba</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choroba.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ choroba.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('choroba_index') }}">lista chorób</a>
    <a href="{{ path('choroba_edit', {'id': choroba.id}) }}">edytuj</a>
    <a href="{{ app.request.headers.get('referer') }}">powrót {{ sciezka }}</a>
    <br>
    

    {{ include('choroba/_delete_form.html.twig') }}
{% endblock %}
