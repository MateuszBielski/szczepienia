{% extends 'base.html.twig' %}

{% block title %}Telefon{% endblock %}

{% block body %}
    <h1>Telefon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telefon.id }}</td>
            </tr>
            <tr>
                <th>Numer</th>
                <td>{{ telefon.numer }}</td>
            </tr>
            <tr>
                <th>Właściciel</th>
                <td>{{ telefon.wlasciciel.imie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('telefon_index') }}">back to list</a>

    <a href="{{ path('telefon_edit', {'id': telefon.id}) }}">edit</a>

    {{ include('telefon/_delete_form.html.twig') }}
{% endblock %}
