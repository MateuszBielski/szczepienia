<form method="post" action="{{ path('szczepionka2_delete', {'id': szczepionka2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ szczepionka2.id) }}">
    <button class="btn">Delete</button>
</form>
